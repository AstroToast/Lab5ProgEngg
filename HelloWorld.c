#include <stdio.h>
// cd  /Users/NickyG./Desktop/IntroProgrammingForEngineers/ 
// ^ locates the director it will check and write to
// gcc -o runme HelloWorld.c
// make sure to put the .c ^

int main(void) {
    int i;
    printf("here \n");
    scanf("%d", &i); // this was done to test to see what allows for a scanf to work
    printf("%d \n", i); // I couldn't get the scanf to work using coderunner
    
    return 0;
}